


// Replace the placeholder method with the actual method to send OTP via SMS using the third-party SMS gateway service of your choice

const sendOtp = (mobileNumber, otp) => {
    // Implement the logic to send OTP via SMS using the SMS gateway service
    // Example:
    const message = `Your OTP is: ${otp}`;
  };
  
  module.exports = sendOtp;